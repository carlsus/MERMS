@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@if (SignInManager.IsSignedIn(User))
{
<div class="navbar-collapse collapse order-1 order-md-0 dual-collapse2">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
                    Task
                </a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" asp-controller="ApprehensionConfiscations" asp-action="Index">Apprehension &amp; Confiscation </a>
                    <a class="dropdown-item" asp-controller="Confiscations" asp-action="Index">Confiscation with Case Filed in Court</a>
                    <a class="dropdown-item" asp-controller="DonatedConfiscateds" asp-action="Index">Donated Confiscated CHW</a>
                    <a class="dropdown-item" asp-controller="ApprehensionConfiscations" asp-action="Index">Price Monitoring Report</a>
                    <a class="dropdown-item" asp-controller="ApprehensionConfiscations" asp-action="Index">Multi-Forest Protection Comimittee Meetings</a>
                    
                </div>
            </li>
        </ul>
</div>
<div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
                    
    <ul class="navbar-nav">


        <li class="nav-item">
            <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
        </li>
        <li class="nav-item">
            <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>

    </ul>
</div>
}
else
{
    @*<li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>*@
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}

